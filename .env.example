SERVICE_NAME=fininfra-double-entry-ledger

# Local app connecting to the PostgreSQL container from the host machine.
# If your docker-compose.yml maps PostgreSQL to a different host port, update this value.
DATABASE_URL=postgresql://fininfra:fininfra@localhost:55432/fininfra
